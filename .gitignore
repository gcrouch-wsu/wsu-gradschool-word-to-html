__pycache__/
*.pyc
*.pyo
.pytest_cache/
.claude/
*.docx
*.heading-map.json
archive/

# Manual content and deployment artifacts. These carry the actual text of live
# policy manuals (or a site's deployed assets) and must never land in the repo.
# Note *.docx above does NOT cover a DOCX carried inside a session bundle, which
# is why the zip pattern is listed separately.
*_session*.zip
manual_fragment*.html
manual_standalone*.html
*_FINAL.html
Faculty_Manual*.html
Faculty_Manual*.md
GSPP*.md
GSPP*.html
*.session.json
facsen*
